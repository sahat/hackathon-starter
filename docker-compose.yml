services:
  web:
    # Build the production image
    build:
      context: .
      dockerfile: ./Dockerfile # Use production Dockerfile (multi-stage optimized)
    container_name: starter

    # Map container port to host
    ports:
      - '8080:8080'

    # Environment variables
    environment:
      - NODE_ENV=production
