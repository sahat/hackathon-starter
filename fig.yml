app:
  image: dockerfile/nodejs
  volumes:
    - ./:/app/
  working_dir: /app/
  command: npm start
  ports:
    - "3000:3000"
  links:
    - mongodb
mongodb:
  image: mongo
  command: mongod --smallfiles