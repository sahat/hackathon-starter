extends layout

block content
  h1=t("home.headline")
  p.lead
    !=t("home.description")
    br
    !=t("home.descriptionMore")

  if user
    .alert.alert-success.animated.flipInX
      i.fa.fa-check
      strong=t('common.sccess')
      !=t("common.logged_in")